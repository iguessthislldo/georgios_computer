// Fibonacci Sequence Computation

// Iteration Values
= $0 0 // Current Iteration
= $1 7 // Break on 7
// $2 will be used for loop

// Initial Values
// 0, 1
= $3 0
= $4 1

// Main Loop
LOOP:

// Add pair
+ $3 $3 $4
+ $4 $3 $4

// Repeat or break
+ $0 $0 1 // $0 = $0 + 1
< $2 $0 $1 // $2 = $0 < $1
if LOOP $2 // LOOP if $2

// Result should be 90, e9 (144, 233 in base 10)
// To continue would overflow the registers

// Done
halt
